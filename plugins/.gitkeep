# This directory stores uploaded plugin .so files at runtime
# The directory is created automatically by the API server if it doesn't exist
# .so files in this directory are ignored by git (see .gitignore)
